version: "3.9"

services:

  ai-factory:

    build:
      context: .
      dockerfile: docker/Dockerfile

    container_name: ai-factory

    volumes:
      - ./:/workspace
      - ~/.ssh:/root/.ssh:ro

    env_file:
      - .env

    tty: true