version: "3.9"

services:

  ai-factory:

    build:
      context: .
      dockerfile: docker/Dockerfile

    container_name: ai-factory

    volumes:
      - ./:/workspace
      - ~/.ssh:/home/factory/.ssh:ro
      - ~/.claude-factory:/home/factory/.claude

    env_file:
      - .env

    tty: true